[market_donut]
label = Market Depth Donut
description = Nested donut chart showing bet categories and markets with volume
default_height = 500
allow_user_selection = true
disabled = 0
search_fragment = | stats sum(volume) as volume by category, market
