[bet_radar]
label = Bet Type Radar Chart
description = Spider/radar chart showing distribution of bet types for World Cup matches
default_height = 500
allow_user_selection = true
disabled = 0
search_fragment = | stats count as volume by bet_type | eventstats avg(volume) as avg_volume
