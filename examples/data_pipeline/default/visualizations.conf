[data_pipeline]
label = Data Pipeline
description = Animated data pipeline showing ingestion volume flowing from sources into Splunk
default_height = 400
allow_user_selection = true
disabled = 0
search_fragment = | stats count by index | rename index as name, count as volume
