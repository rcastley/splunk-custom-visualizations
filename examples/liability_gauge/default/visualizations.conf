[liability_gauge]
label = Liability Exposure Gauge
description = Multi-ring concentric gauge showing liability exposure with RAG status
default_height = 500
allow_user_selection = true
disabled = 0
search_fragment = | stats sum(exposure) as exposure max(threshold) as threshold by category
