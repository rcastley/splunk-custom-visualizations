[live_ticker]
label = Live Ticker
description = Broadcast-style scrolling ticker showing recent events with custom field labels and live time-ago indicator.
default_height = 70
allow_user_selection = true
disabled = 0
search_fragment = | stats latest(_time) as _time latest(field1) as field1 latest(field2) as field2 by _time
