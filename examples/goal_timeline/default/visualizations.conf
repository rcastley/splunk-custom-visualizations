[goal_timeline]
label = Goal Event Timeline
description = Betting volume area chart with goal, red card, and penalty markers
default_height = 350
allow_user_selection = true
disabled = 0
search_fragment = | stats sum(volume) as volume by minute | table minute volume event
