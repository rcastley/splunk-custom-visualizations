[goal_timeline]
label = Goal Event Timeline
description = Horizontal timeline showing betting volume as an area chart with event markers for goals, red cards, penalties, and VAR reviews
default_height = 350
allow_user_selection = true
disabled = 0
search_fragment = | stats sum(volume) as volume by minute | eval event=if(isnotnull(event),event,"") | eval event_type=if(isnotnull(event_type),event_type,"")
