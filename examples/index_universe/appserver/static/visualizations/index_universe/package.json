{
  "name": "index-universe-viz",
  "version": "1.0.0",
  "description": "Orbital bubble chart of Splunk indexes — size is disk usage, color is event count, orbit is retention age",
  "scripts": {
    "build": "webpack --mode production",
    "dev": "webpack --mode development --watch"
  },
  "devDependencies": {
    "webpack": "^5.90.0",
    "webpack-cli": "^5.1.4"
  }
}
