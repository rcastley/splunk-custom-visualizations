[id]
name = index_universe
version = 1.0.0

[install]
is_configured = true
build = 1

[package]
id = index_universe

[ui]
is_visible = true
label = Index Universe

[launcher]
author = rcastley
description = Orbital bubble chart showing Splunk indexes as celestial bodies — size is disk usage, color is event count, orbit distance is retention age
version = 1.0.0
