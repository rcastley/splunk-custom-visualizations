[arcade_leaderboard]
label = Arcade Leaderboard
description = Arcade-style leaderboard with neon colors and CRT scanlines
default_height = 500
allow_user_selection = true
disabled = 0
search_fragment = | stats latest(score) as score by player_name | sort -score
