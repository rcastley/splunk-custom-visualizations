[network_topology]
label = Network Topology
description = Interactive network topology visualization showing nodes and connections with force-directed layout
default_height = 500
allow_user_selection = true
disabled = 0
search_fragment = | stats count by source dest | appendcols [| stats values(status) as status by source]
