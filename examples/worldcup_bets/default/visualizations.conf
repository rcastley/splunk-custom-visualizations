[worldcup_bets]
label = World Cup Bets Pulse
description = Football pitch-themed radial pulse gauge for real-time World Cup betting volume
default_height = 500
allow_user_selection = true
disabled = 0
search_fragment = | stats count as bets_per_minute | eval match_name="ENGLAND vs FRANCE", peak_bpm=12450, prev_bpm=round(random() % 10000)
