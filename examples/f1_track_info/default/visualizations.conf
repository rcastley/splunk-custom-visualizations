[f1_track_info]
label = F1 Track Info
description = Track info card with miniature silhouette colored by sector, sector boundary markers, track name, length, and turn count
default_height = 350
allow_user_selection = true
disabled = 0
search_fragment = index="data_drivers_f1_2025" sourcetype="SessionData" | stats latest(track_id) as track_id latest(track_length) as track_length latest(sector2_lap_distance_start) as sector2_start latest(sector3_lap_distance_start) as sector3_start | table track_id track_length sector2_start sector3_start
