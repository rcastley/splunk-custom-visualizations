[match_heatmap]
label = Match Heatmap Grid
description = Grid visualization showing World Cup matches with cells colored by betting volume intensity
default_height = 500
allow_user_selection = true
disabled = 0
search_fragment = | stats sum(volume) as volume by match, time_slot
