[custom_single_value]
label = Custom Single Value
description = Display any field value with configurable colour, alignment, weight, and optional label
default_height = 100
allow_user_selection = true
disabled = 0
search_fragment = | stats latest(field_name) as value
